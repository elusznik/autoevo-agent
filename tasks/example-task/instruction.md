# Example Task

Fix the bug in the provided Python code.

The code in `app.py` has a function `calculate_sum` that should return the sum of all numbers from 1 to n, but it currently returns the wrong result.

Run the tests to see the failure, then fix the bug.

## Success Criteria

The test should pass:
```
python test_app.py
```

## Files

- `app.py` - The buggy implementation
- `test_app.py` - Test file (do not modify)
